import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['source.unsplash.com','png.pngtree.com','res.cloudinary.com','example.com'], // Allow external images from Unsplash

  },
  /* config options here */
};

export default nextConfig;
